<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".Fragment_OrderDetails"
    android:background="@color/colorBackground">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Order Description"
            android:textSize="40dp"
            android:textColor="@color/colorDarkText"
            android:layout_marginBottom="15dp"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/Order_Details_RCV_description"
            android:layout_width="match_parent"
            android:layout_height="385dp">

        </androidx.recyclerview.widget.RecyclerView>

        <TextView
            android:id="@+id/Order_Details_RCV_totalPrice"
            android:layout_width="match_parent"
            android:layout_height="126dp"
            android:text="Total_Price"
            android:textSize="30dp" />

    </LinearLayout>
    <ImageView
        android:layout_width="94dp"
        android:layout_height="136dp"
        android:layout_gravity="bottom|right"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="30dp"
        app:srcCompat="@drawable/cocktail"
        android:rotation="@android:integer/config_mediumAnimTime"/>

</FrameLayout>